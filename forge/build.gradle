apply from: "https://raw.githubusercontent.com/Flemmli97/GradleScripts/arch-loom-v2/forge.gradle"

loom {
    runs {
        data {
            data()
            programArgs "--all", "--mod", "simplequests"
            programArgs "--output", project(":common").file("src/generated/resources").absolutePath
            property("ExampleGenPath", project.rootProject.file("Example-Datapack/Datapack").absolutePath)
        }
    }
    forge {
        mixinConfigs = ["simplequests.mixins.json"]
    }
}

repositories {
    mavenCentral()
    maven {
        name = "FTB"
        url = "https://maven.saps.dev/minecraft"
    }
    maven {
        name = "LinguaBib"
        url "https://gitlab.com/api/v4/projects/21830712/packages/maven"
    }
}

dependencies {
    modCompileOnly("dev.ftb.mods:ftb-ranks-forge:${ftb_ranks}")

    // Jar-Jar only in the remapped jar but we cant use that
    modRuntimeOnly "io.github.flemmli97:lingua_bib:${lingua_bib}-forge"
    compileOnly(include(project(path: ":forge-api")))
    runtimeOnly(project(path: ":forge-api", configuration: "shadowBundle"))
}

shadowJar {
    exclude "**/forge/test/**"
}